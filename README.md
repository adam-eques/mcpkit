# mcpkit

A Model Context Protocol (MCP) server written in Go, using only the standard
library. Work in progress.

## Status

Early scaffolding. See `docs/` as it lands for architecture and protocol notes.
